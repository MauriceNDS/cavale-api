package com.cavale.training.domain;

/** Week character in the periodization cycle (3:1 load/deload, shock blocks, taper…). */
public enum WeekType {
    RECOVERY, TRANSITION, BUILD, DELOAD, SHOCK, TAPER, RACE
}
