# Test-only overrides. The main config now REQUIRES CAVALE_JWT_SECRET (no
# insecure default), so the test context supplies a throwaway HS256 secret
# here — long enough for the >= 32-byte guard, never used outside tests.
cavale.security.jwt.secret=test-only-jwt-secret-0123456789-abcdefghij
